import React, { FC } from "react";
import styles from "../styles/Footer.module.css";

const Footer: FC = () => {
  return (
    <footer className={styles.footer}>Powered by Prolitigation LLC</footer>
  );
};

export default Footer;
